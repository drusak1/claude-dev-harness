# Progress log — <project name>

Newest first. Updated at every clock-out (`/handoff`).

The test for an entry: **could the next session continue from this alone?**

## YYYY-MM-DD — session N

- **Done:**
- **In progress:** (ledger id + approach-class)
- **Blocked:** (with the actual failure output, not a summary of it)
- **NEXT STEP:**
- **Baseline:** `<check command>` PASS/FAIL at `<commit>`
- **Scout queue:** none | forced-scout on `<class>` — clock-in must honour it
