# Quality document

Answers "is the harness getting stronger or weaker over time" — the per-session
rubric only answers "was this session good". Updated during `/reflect weekly`.

## Grades (A-D)

| Area | Verification | Agent legibility | Notes |
|---|---|---|---|
| ledger | A | A | 16 tests, one per discipline |
| projects registry | B | A | validated, but `check` commands are untested until run |
| hooks | B | A | session_start + pre_change; no automated test yet |
| skills | — | A | prose; quality shows up as friction in the observation log |

## Benchmark snapshots

| Date | make check | Tests | Notes |
|---|---|---|---|
| 2026-07-18 | PASS | 16 passed | bootstrap |

## Simplification log

Every harness component encodes an assumption about what the model cannot do on
its own. Those assumptions go stale as models improve. Monthly: disable one
component, run a normal work session, and remove it permanently if nothing
degrades. A harness that only grows is a harness nobody will keep using.

| Date | Component | Action | Result |
|---|---|---|---|
| — | — | — | — |

## Promotion ledger

Rule 3 of `/reflect`: a friction seen twice becomes a mechanical check, never
more prose. Record each promotion so the harness's growth is auditable.

| Date | Observation | Promoted to |
|---|---|---|
| — | — | — |
